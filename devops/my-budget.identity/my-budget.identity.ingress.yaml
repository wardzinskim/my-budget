apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-budget-identity-ingress
  namespace: my-budget
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: ingress-external
  rules:
    - host: my-budget.xyz
      http:
        paths:
          - path: /identity
            pathType: Prefix
            backend:
              service:
                name: my-budget-identity-service
                port:
                  number: 80
  tls:
    - hosts:
        - my-budget.xyz
      secretName: my-budget-identity-tls
