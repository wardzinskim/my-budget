apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-budget-identity
  namespace: my-budget
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-budget-identity
  template:
    metadata:
      labels:
        app: my-budget-identity
        app.kubernetes.io/name: my-budget-identity
    spec:
      serviceAccountName: mybudget
      containers:
        - name: my-budget-identity
          image: ghcr.io/wardzinskim/my-budget.identity:main
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: secrets
              mountPath: "/app/appsettings.json"
              subPath: "appsettings.json"
              readOnly: true
      volumes:
        - name: secrets
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "spc-vault-mybudget-identity"
